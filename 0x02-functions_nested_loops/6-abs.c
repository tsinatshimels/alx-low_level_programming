#include "main.h"

/**
 * _abs - computes tha absolute value of an intege
 * @t: The integer to be computed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
